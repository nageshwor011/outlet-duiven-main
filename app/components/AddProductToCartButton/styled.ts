import styled from "@emotion/styled";
import { SubmitBtn } from "~/components/Form/SubmitBtn";

export const AddToCartBtn = styled(SubmitBtn)`
  padding: var(--space-2);
  gap: var(--space-2);
  display: flex;
  font-size: 2.4rem;
  position: relative;
  z-index: 1;
`;
