import styled from "@emotion/styled";

export const Root = styled.div`
  position: fixed;
  background: #fff;
  border-top: 0.1rem solid var(--color-gray-13);
  padding: var(--space-4) 0;
  left: 0;
  bottom: 0;
  right: 0;
  z-index: var(--z-index-fixed-banner);
`;
