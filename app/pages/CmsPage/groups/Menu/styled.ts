import styled from "@emotion/styled";
import { Container } from "~/components/Container";

export const GridRow = styled(Container)`
  display: flex;
  flex-direction: column;
  gap: var(--space-8);
`;
